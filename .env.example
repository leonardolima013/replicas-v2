# ===== BANCO DE DADOS DE METADADOS =====
DB_HOST=db_metadados
DB_PORT=5432
POSTGRES_USER=pgroot
POSTGRES_PASSWORD=pg@root
POSTGRES_DB=replicas_metadados_db

# ===== JWT CONFIGURATION =====
JWT_SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=300

# ===== OPCIONAL: DATABASE_URL =====
# Se preferir usar SQLite localmente (desenvolvimento):
# DATABASE_URL=sqlite:///./metadata.db

# Se usar PostgreSQL (produção):
# DATABASE_URL=postgresql://pgroot:pg@root@db_metadados:5432/replicas_metadados_db
